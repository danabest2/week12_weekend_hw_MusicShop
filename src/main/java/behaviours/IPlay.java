package behaviours;
//Create a IPlay interface that could be applied to instruments. This should have a play method
// that returns the sound of the instrument being played as a String.
public interface IPlay {
     String play(String Sound);
}
